<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark variant mirrors base: the vault is always nocturnal. -->
    <style name="Base.Theme.MyGalleryVault" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/sanity_canvas</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
