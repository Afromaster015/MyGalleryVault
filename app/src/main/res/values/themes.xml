<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme — sanity is dark-only; window matches canvas #0B0B0B. -->
    <style name="Base.Theme.MyGalleryVault" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/sanity_canvas</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.MyGalleryVault" parent="Base.Theme.MyGalleryVault" />
</resources>
